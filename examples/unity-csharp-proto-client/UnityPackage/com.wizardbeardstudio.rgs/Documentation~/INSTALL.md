# Install

1. Copy `UnityPackage/com.wizardbeardstudio.rgs` into your Unity project package source or local UPM package folder.
2. Ensure protobuf/gRPC runtime assemblies are available in your project.
3. Add `RgsClientBootstrap` to a GameObject and configure `RgsClientConfig` in Inspector.
